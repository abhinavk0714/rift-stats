# Copy to .env and adjust. In dev, Vite proxies /api to backend (default :8000).
# Development (with proxy): use /api so requests go to Vite dev server and get proxied.
VITE_API_BASE_URL=/api

# Production example (replace with your deployed backend URL):
# VITE_API_BASE_URL=https://your-api.example.com
